# LocaPick V2 카카오 로그인 연동 트러블슈팅 일지 (kakaoLoginSolved)

본 문서는 LocaPick V2 프로젝트에 카카오 OAuth 로그인을 연동하는 과정에서 발생한 주요 에러들과 그 원인, 그리고 완벽한 해결 방법을 정리한 문서입니다.

---

## 1. KOE004 에러 (카카오 로그인 비활성화)
- **증상:** 카카오 로그인 버튼을 누르면 카카오 측에서 `KOE004` 에러 페이지를 띄움.
- **원인:** 카카오 개발자 콘솔(Kakao Developers)에서 해당 앱의 '카카오 로그인' 기능 설정이 활성화(ON)되어 있지 않았음.
- **해결:** 카카오 개발자 콘솔 ➔ [내 애플리케이션] ➔ [제품 설정] ➔ [카카오 로그인] 메뉴에서 상태를 '활성화(ON)'로 변경함.

---

## 2. 500 Internal Server Error (Client Secret 누락)
- **증상:** 로그인 콜백에서 백엔드가 카카오 서버와 통신할 때 500 에러 발생.
- **원인:** 카카오 앱 설정에 보안(Client Secret)이 설정되어 있으나, 백엔드 `backend/.env` 파일에 `KAKAO_CLIENT_SECRET` 값이 비어 있어서 토큰 교환 권한을 거부당함.
- **해결:** 카카오 개발자 콘솔에서 시크릿 키를 발급/확인하여 `.env` 파일에 정상 기입 후 빌드하여 해결.

---

## 3. KOE320 에러 (인가 코드 중복 사용 문제)
- **증상:** 한 번 로그아웃 한 뒤 다시 로그인하려고 할 때, 또는 로그인 과정 중 순식간에 에러 페이지로 튕기면서 `KOE320 (authorization code not found)` 에러가 발생함.
- **원인:** 카카오가 발급하는 인가 코드(Code)는 1회용인데, 이것이 2번 이상 백엔드로 요청되면서 발생.
  1. **브라우저 302 캐싱 현상:** 프론트엔드가 `/api/auth/kakao`로 이동할 때, 브라우저가 옛날에 받았던 경로를 캐싱하여 카카오 서버를 거치지 않고 옛날 코드를 백엔드로 던져버림.
  2. **유령 중복 요청 (Phantom Duplicate Requests):** 크롬 확장 프로그램(AdBlock 등)이나 브라우저 자체의 프리패치(Pre-fetch) 기능 때문에, 백엔드 콜백 주소(`/auth/kakao/callback?code=...`)로 순식간에 2번 연속 똑같은 GET 요청이 들어옴. 첫 번째는 성공하지만, 두 번째 요청이 카카오 서버에 튕기면서 에러를 발생시키고 브라우저를 에러 페이지로 이동시킴.
- **해결:**
  1. 프론트엔드 `Login.jsx`의 호출 주소 뒤에 타임스탬프(`?t=${new Date().getTime()}`)를 붙여 브라우저 캐싱을 원천 차단.
  2. 백엔드 `AuthController.java`에 `ConcurrentHashMap`을 이용한 **방어막 캐시(Cache)** 로직을 추가. 이미 토큰 교환에 성공한 Code가 다시 들어오면 카카오 서버로 보내지 않고 기존에 생성해둔 '로그인 성공 리다이렉트 URL'을 똑같이 반환하도록 조치.

---

## 4. 500 HeadersTooLargeException 에러 (프로필 사진 용량 초과)
- **증상:** KOE320 에러를 잡은 직후, 정상 로그인이 되어야 할 시점에 브라우저에 `500 (Internal Server Error)` 페이지만 뜨고 멈춤. 백엔드 로그에는 `HeadersTooLargeException`이 기록됨.
- **원인 (로컬 로그인과의 통신 방식 차이):**
  - 사용자가 이전에 직접 업로드한 거대한 프로필 이미지(수 MB 크기의 Base64 텍스트)가 DB에 저장되어 있었음.
  - **로컬 로그인**은 성공 시 백엔드가 프론트엔드에게 무제한 용량의 `JSON Body`로 데이터를 응답하므로 거대한 사진도 문제가 없었음.
  - 하지만 **카카오 로그인(OAuth)** 은 백엔드가 프론트엔드 화면으로 유저를 강제 이동(Redirect) 시켜야 하므로, URL 주소 뒤 파라미터(`?token=...&profileImageUrl=...`)에 모든 데이터를 욱여넣어야 했음.
  - 내장 톰캣(Tomcat) 서버는 보안상 URL 및 헤더 길이를 최대 8KB로 제한하므로, 수 MB의 사진 텍스트가 URL에 붙자 한도를 초과하여 서버가 뻗어버림.
- **해결:**
  1. **백엔드 (`AuthController.java`):** 프로필 이미지 문자열 길이가 1,000자를 넘어가면(Base64 이미지면) URL 파라미터에 값을 빈 칸(`""`)으로 비워서 보냄으로써 헤더 사이즈 초과를 방지.
  2. **프론트엔드 (`KakaoCallback.jsx`):** 콜백 페이지에서 파싱한 `profileImageUrl`이 빈 칸일 경우, 토큰만 먼저 스토어에 저장한 뒤 즉시 백엔드의 `/api/members/me` API를 호출해 내 정보를 조회함. 이를 통해 잘려나간 거대 Base64 프로필 이미지를 덮어써 우측 상단 헤더에도 사진이 정상 노출되도록 '지연 로딩' 방식으로 완벽 재구성.
